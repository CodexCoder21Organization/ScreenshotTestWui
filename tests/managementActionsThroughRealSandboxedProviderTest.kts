@file:WithArtifact("screenshottest.wui.buildSandboxClientFixtureResourcesJar()")
@file:WithArtifact("screenshottest.wui.buildMaven()")
@file:WithArtifact("org.eclipse.jetty:jetty-server:11.0.20")
@file:WithArtifact("org.eclipse.jetty:jetty-servlet:11.0.20")
@file:WithArtifact("org.eclipse.jetty:jetty-http:11.0.20")
@file:WithArtifact("org.eclipse.jetty:jetty-io:11.0.20")
@file:WithArtifact("org.eclipse.jetty:jetty-util:11.0.20")
@file:WithArtifact("org.eclipse.jetty:jetty-security:11.0.20")
@file:WithArtifact("jakarta.servlet:jakarta.servlet-api:5.0.0")
@file:WithArtifact("foundation.url:resolver:0.0.1261")
@file:WithArtifact("foundation.url:protocol:0.0.532")
@file:WithArtifact("net.javadeploy.sjvm:libSJVM-jvm:0.0.50")
@file:WithArtifact("net.javadeploy.sjvm:avianStdlibHelper-jvm:0.0.50")
@file:WithArtifact("net.javadeploy.sjvm:stdlibHelperCommon-jvm:0.0.50")
@file:WithArtifact("org.ow2.asm:asm:9.8")
@file:WithArtifact("org.ow2.asm:asm-commons:9.8")
@file:WithArtifact("org.ow2.asm:asm-util:9.8")
@file:WithArtifact("org.ow2.asm:asm-tree:9.8")
@file:WithArtifact("community.kotlin.clocks.simple:community-kotlin-clocks-simple:0.0.11")
@file:WithArtifact("community.kotlin.clocks.hierarchical:community-kotlin-clocks-hierarchical:0.0.6")
@file:WithArtifact("community.kotlin.rpc:protocol-api:0.0.2")
@file:WithArtifact("community.kotlin.rpc:protocol-impl:0.0.11")
@file:WithArtifact("screenshottest.api:screenshottest-api:0.0.3")
@file:WithArtifact("org.json:json:20250517")
@file:WithArtifact("com.squareup.okio:okio-jvm:3.4.0")
@file:WithArtifact("org.jetbrains.kotlin:kotlin-stdlib:1.9.22")
@file:WithArtifact("org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.9.22")
@file:WithArtifact("org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.9.22")
@file:WithArtifact("org.jetbrains.kotlin:kotlin-reflect:1.9.22")
@file:WithArtifact("org.jetbrains.kotlinx:kotlinx-coroutines-core-jvm:1.8.0")
@file:WithArtifact("org.slf4j:slf4j-api:1.7.36")
@file:WithArtifact("org.slf4j:slf4j-simple:2.0.9")
@file:WithArtifact("community.kotlin.libp2p:jvm-libp2p:1.3.0-codexcoder21-snapshot-27")
@file:WithArtifact("com.google.protobuf:protobuf-java:3.25.1")
@file:WithArtifact("tech.pegasys:noise-java:22.1.0")
@file:WithArtifact("io.netty:netty-buffer:4.2.10.Final")
@file:WithArtifact("io.netty:netty-codec:4.2.10.Final")
@file:WithArtifact("io.netty:netty-codec-http:4.2.10.Final")
@file:WithArtifact("io.netty:netty-codec-http2:4.2.10.Final")
@file:WithArtifact("io.netty:netty-common:4.2.10.Final")
@file:WithArtifact("io.netty:netty-handler:4.2.10.Final")
@file:WithArtifact("io.netty:netty-resolver:4.2.10.Final")
@file:WithArtifact("io.netty:netty-transport:4.2.10.Final")
@file:WithArtifact("io.netty:netty-transport-classes-epoll:4.2.10.Final")
@file:WithArtifact("io.netty:netty-transport-classes-kqueue:4.2.10.Final")
@file:WithArtifact("io.netty:netty-transport-native-unix-common:4.2.10.Final")
@file:WithArtifact("org.bouncycastle:bcpkix-jdk18on:1.78.1")
@file:WithArtifact("org.bouncycastle:bcprov-jdk18on:1.78.1")
@file:WithArtifact("org.bouncycastle:bcutil-jdk18on:1.78.1")
@file:WithArtifact("com.google.guava:guava:33.2.0-jre")
@file:WithArtifact("com.google.guava:failureaccess:1.0.2")
@file:WithArtifact("community.kotlin.observable:core-jvm:0.3.21")
@file:WithArtifact("util.stacktrace:util-stacktrace:0.0.1")
@file:WithArtifact("kompile.effects:kompile-effects:0.0.1")
@file:WithArtifact("community.kotlin.logging:community-kotlin-logging:0.0.1")
@file:WithArtifact("util.cache.hashtofile:hash-to-file-cache:0.0.4")
@file:WithArtifact("cache.key.builder:cache-key-builder:0.0.3")
@file:WithArtifact("file.utils:file-utils:0.0.1")
@file:WithArtifact("util.string:stringutils:0.0.1")
@file:WithArtifact("resource.arena:resource-arena:0.0.1")
@file:WithArtifact("kotlinc.diagnostic.collector:kotlinc-diagnostic-collector:0.0.1")
@file:WithArtifact("kompile.buildrule.enhancer:kompile-buildrule-enhancer:0.0.1")
@file:WithArtifact("refcounted:refcounted:0.0.1")
@file:WithArtifact("util.jar:jarutils:0.0.1")
@file:WithArtifact("kompile.interfaces.internal:kompile-interfaces-internal:0.0.1")
@file:WithArtifact("tools.kotlin.build.compile.interfaces.executionenvironment:tools-kotlin-build-compile-interfaces-executionenvironment:0.0.1")
@file:WithArtifact("tools.kotlin.build.compile.interfaces.diagnostics:tools-kotlin-build-compile-interfaces-diagnostics:0.0.1")
@file:WithArtifact("community.kotlin.coursier.bldbinary:community-kotlin-coursier-bldbinary:0.0.1")
@file:WithArtifact("io.get-coursier:interface:0.0.0+481-d9800bd9-SNAPSHOT")
@file:WithArtifact("io.get-coursier:coursier-util_2.13:2.1.30")
@file:WithArtifact("io.get-coursier:coursier-core_2.13:2.1.30")
@file:WithArtifact("io.get-coursier:coursier_2.13:2.1.30")
@file:WithArtifact("io.get-coursier:coursier-cache_2.13:2.1.30")
@file:WithArtifact("algebraic.effects.v1.runtime:algebraic-effects-v1-runtime:0.0.1")
@file:WithArtifact("w3wallet.api:w3wallet-api:0.0.1")
@file:WithArtifact("build.kotlin.annotations:build-kotlin-annotations:0.0.4")
@file:WithArtifact("org.jetbrains.kotlin:kotlin-test:1.9.22")

package screenshottest.wui
import kotlin.test.*

import build.kotlin.withartifact.WithArtifact
import foundation.url.protocol.BootstrapPeer
import foundation.url.protocol.Libp2pPeer
import foundation.url.protocol.ServiceHandler
import foundation.url.protocol.ServiceInfrastructureException
import foundation.url.resolver.UrlProtocol2
import foundation.url.resolver.UrlResolver
import screenshottest.api.ScreenshotTestApi
import java.net.HttpURLConnection
import java.net.URL
import java.security.MessageDigest
import java.util.concurrent.Callable
import java.util.concurrent.Executors
import java.util.concurrent.TimeUnit
import kotlin.test.assertEquals
import kotlin.test.assertTrue

/**
 * The management actions against a REAL `url://` provider through the WUI's SJVM sandbox proxy
 * (`UrlResolver.openSandboxedConnection`): the provider's own IllegalStateException /
 * IllegalArgumentException must reach the WUI as the service-reported class on the resulting
 * SandboxException, so a wrong-state cancel is 409, an unknown session is 404, and a rejected
 * max-workers value is 400 -- each banner carrying the service's own message -- while any other
 * reported class stays 502 (500 for an image), and a hosting-layer ServiceInfrastructureException,
 * which the service did not report, is 502 rather than a 409.
 */
@build.kotlin.annotations.Timeout(120)
fun managementActionsThroughRealSandboxedProviderTest() {
    val serviceId = "screenshottest-wui-real-provider-actions-${System.nanoTime()}"
    val classLoader = Thread.currentThread().contextClassLoader
    fun requiredResource(name: String): ByteArray =
        requireNotNull(classLoader.getResourceAsStream(name)) {
            "The sandbox fixture resource '$name' is missing from the test classpath."
        }.use { it.readBytes() }
    val implementationJar = requiredResource("sandbox-client-impl.jar")
    val stdlibJar = requiredResource("stdlib.jar")
    val implementationJarDigest = MessageDigest.getInstance("SHA-256").digest(implementationJar)
        .joinToString("") { "%02x".format(it.toInt() and 0xff) }
    val sessionsJson = """[{"sessionId":"sess-c1","label":"completed one","mode":"compare","state":"COMPLETED","createdAt":1735685900000,"rendererVersion":"chromium-1228","queuePosition":null,"startedAt":1735686000000,"finishedAt":1735686090000}]"""
    val poolJson = """{"maxWorkers":3,"activeWorkers":0,"queuedSessions":0,"running":[],"queued":[]}"""
    val handler = object : ServiceHandler {
        override suspend fun handleRequest(path: String, params: Map<String, Any?>, metadata: Map<String, String>): Any? =
            when (path) {
                "listSessions" -> mapOf("json" to sessionsJson)
                "getWorkerPoolStatus" -> mapOf("json" to poolJson)
                "cancelSession" -> when (params["sessionId"]) {
                    "sess-c1" -> throw IllegalStateException("Session 'sess-c1' is COMPLETED; only RUNNING sessions can be cancelled.")
                    "sess-unknown" -> throw IllegalArgumentException("No screenshot session with id 'sess-unknown'.")
                    "sess-infra" -> throw ServiceInfrastructureException("handler session expired")
                    else -> throw UnsupportedOperationException("backend exploded")
                }
                "deleteSession" -> throw IllegalStateException("Session '${params["sessionId"]}' is RUNNING; cancel it before deleting it.")
                "setMaxWorkers" -> when ((params["maxWorkers"] as? Number)?.toInt()) {
                    7 -> throw UnsupportedOperationException("the pool cannot be resized while draining")
                    else -> throw IllegalArgumentException("maxWorkers must be between 1 and 16, but was ${params["maxWorkers"]}.")
                }
                "getImageChunk" -> throw UnsupportedOperationException("image store offline")
                else -> throw IllegalArgumentException("Unexpected RPC '$path'.")
            }
        override fun onShutdown() = Unit
        override fun getImplementationJar(): ByteArray = implementationJar
        override fun getImplementationClassName(): String = "screenshottest.wui.testfixtures.SandboxedScreenshotTestClient"
        override fun getImplementationJarDigest(): String = implementationJarDigest
        override fun getStdlibJar(): ByteArray = stdlibJar
        override fun supportsSandboxedExecution(): Boolean = true
    }
    fun post(port: Int, path: String, form: String): HttpURLConnection {
        val conn = URL("http://localhost:$port$path").openConnection() as HttpURLConnection
        conn.instanceFollowRedirects = false
        conn.requestMethod = "POST"
        conn.doOutput = true
        conn.setRequestProperty("Content-Type", "application/x-www-form-urlencoded")
        conn.outputStream.use { it.write(form.toByteArray(Charsets.UTF_8)) }
        return conn
    }
    fun bodyOf(conn: HttpURLConnection): String =
        (if (conn.responseCode < 400) conn.inputStream else conn.errorStream).bufferedReader().readText()
    val provider = UrlProtocol2(emptyList<BootstrapPeer>(), eagerlyJoinNetwork = false, listenPort = 0)
    var resolver: UrlResolver? = null
    var server: org.eclipse.jetty.server.Server? = null
    try {
        val providerInfo = provider.joinNetwork(alias = "$serviceId-provider")
        provider.registerGlobalService(serviceUrl = "url://$serviceId/", handler = handler)
        val providerPeer = Libp2pPeer.remote(
            peerId = providerInfo.peerId,
            multiaddresses = providerInfo.multiaddresses
                .filter { it.contains("/ip4/") }
                .map { it.replace(Regex("/ip4/[0-9.]+/"), "/ip4/127.0.0.1/") },
            advertisedServices = listOf(serviceId),
        )
        val clientResolver = UrlResolver(UrlProtocol2(listOf(providerPeer), eagerlyJoinNetwork = false, listenPort = 0))
        resolver = clientResolver
        val api = clientResolver.openSandboxedConnection("url://$serviceId/", ScreenshotTestApi::class)
        val wui = createServer(0, api)
        server = wui
        wui.start()
        val port = (wui.connectors[0] as org.eclipse.jetty.server.ServerConnector).localPort

        val conflict = post(port, "/session/cancel", "id=sess-c1&returnTo=list")
        val conflictHtml = bodyOf(conflict)
        assertEquals(409, conflict.responseCode, "A wrong-state cancel through the real sandbox must be 409; page was:\n$conflictHtml")
        assertTrue(conflictHtml.contains("""<span class="banner-text">Could not cancel session &#39;sess-c1&#39;: Session &#39;sess-c1&#39; is COMPLETED; only RUNNING sessions can be cancelled.</span>"""),
            "Expected the service's own message in the banner; page was:\n$conflictHtml")

        val unknown = post(port, "/session/cancel", "id=sess-unknown&returnTo=list")
        val unknownHtml = bodyOf(unknown)
        assertEquals(404, unknown.responseCode, "An unknown session through the real sandbox must be 404; page was:\n$unknownHtml")
        assertTrue(unknownHtml.contains("""<span class="banner-text">Could not cancel session &#39;sess-unknown&#39;: No screenshot session with id &#39;sess-unknown&#39;.</span>"""),
            "Expected the service's own message in the banner; page was:\n$unknownHtml")

        val delete = post(port, "/session/delete", "id=sess-r1&returnTo=list")
        val deleteHtml = bodyOf(delete)
        assertEquals(409, delete.responseCode, "A wrong-state delete through the real sandbox must be 409; page was:\n$deleteHtml")
        assertTrue(deleteHtml.contains("""<span class="banner-text">Could not delete session &#39;sess-r1&#39;: Session &#39;sess-r1&#39; is RUNNING; cancel it before deleting it.</span>"""),
            "Expected the service's own message in the banner; page was:\n$deleteHtml")

        val maxWorkers = post(port, "/workers/max", "maxWorkers=99")
        val maxWorkersHtml = bodyOf(maxWorkers)
        assertEquals(400, maxWorkers.responseCode, "A rejected max-workers value through the real sandbox must be 400; page was:\n$maxWorkersHtml")
        assertTrue(maxWorkersHtml.contains("The screenshot service rejected max workers 99: maxWorkers must be between 1 and 16, but was 99."),
            "Expected the service's own message in the banner; page was:\n$maxWorkersHtml")

        val other = post(port, "/session/cancel", "id=sess-other&returnTo=list")
        val otherHtml = bodyOf(other)
        assertEquals(502, other.responseCode, "A failure the service reports as any other class stays 502; page was:\n$otherHtml")
        assertTrue(otherHtml.contains("""<span class="banner-text">The screenshot service failed to cancel session &#39;sess-other&#39;: backend exploded</span>"""),
            "Expected the service's own message in the 502 banner; page was:\n$otherHtml")

        // A hosting-layer failure is not the service's exception: 502, never 404/409, even though
        // ServiceInfrastructureException is an IllegalStateException on the provider side.
        val infra = post(port, "/session/cancel", "id=sess-infra&returnTo=list")
        val infraHtml = bodyOf(infra)
        assertEquals(502, infra.responseCode, "An infrastructure failure through the real sandbox must be 502; page was:\n$infraHtml")
        assertTrue(infraHtml.contains("The screenshot service failed to cancel session &#39;sess-infra&#39;: "),
            "Expected the 502 banner prefix; page was:\n$infraHtml")
        assertTrue(infraHtml.contains("handler session expired"), "Expected the failure text in the banner; page was:\n$infraHtml")

        val workersFailure = post(port, "/workers/max", "maxWorkers=7")
        val workersFailureHtml = bodyOf(workersFailure)
        assertEquals(502, workersFailure.responseCode, "A non-argument max-workers failure must be 502; page was:\n$workersFailureHtml")
        assertTrue(workersFailureHtml.contains("The screenshot service failed to set max workers to 7: the pool cannot be resized while draining"),
            "Expected the service's own message in the 502 banner; page was:\n$workersFailureHtml")

        val image = URL("http://localhost:$port/image?id=sess-c1&key=home&kind=actual").openConnection() as HttpURLConnection
        val imageBody = bodyOf(image)
        assertEquals(500, image.responseCode, "A non-argument image failure must be 500; body was:\n$imageBody")
        assertEquals("Failed to load image for session \"sess-c1\", key \"home\", kind \"actual\": image store offline", imageBody,
            "Expected the service's own message in the image error")
    } finally {
        try { server?.stop() } finally {
            try { resolver?.close() } finally { provider.close() }
        }
    }
}
